<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Free Cycling Nutrition Plan (Carbs, Hydration & Sodium) | RaceFeed</title>
  <meta name="description" content="Optimize your performance! Estimate carbs, hydration, and sodium needs based on your ride conditions — with practical fueling strategies" />
  <meta name="keywords" content="cycling nutrition, fuel plan, carbohydrates, hydration, sodium, race nutrition, cycling calculator" />
  <meta name="author" content="Lucas Vido" />

  <link rel="canonical" href="https://racefeed.com.br/fuelplan.html" />
  <link rel="icon" href="/favicon.ico" />

  <meta property="og:title" content="Cycling Fuel Plan Calculator" />
  <meta property="og:description" content="Optimize your performance! Estimate carbs, hydration, and sodium needs based on your ride conditions — with practical fueling strategies" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://racefeed.com.br/fuelplan.html" />
  <meta property="og:image" content="https://racefeed.com.br/og-fuelplan.webp" />

  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Free Cycling Fueling Plan (Carbs, Hydration & Sodium) | RaceFeed" />
  <meta name="twitter:description" content="Optimize your performance! Estimate carbs, hydration, and sodium needs based on your ride conditions — with practical fueling strategies" />
  <meta name="twitter:image" content="https://racefeed.com.br/og-fuelplan.webp" />

  <!-- Structured Data -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebApplication",
    "name": "Cycling Fuel Plan Calculator",
    "url": "https://racefeed.com.br/fuelplan.html",
    "description": "Estimate carbs, hydration, and sodium needs based on your ride conditions — with practical fueling strategies.",
    "applicationCategory": "SportsApplication",
    "operatingSystem": "Any",
    "offers": { "@type": "Offer", "price": "0", "priceCurrency": "BRL" },
    "author": { "@type": "Person", "name": "Lucas Vido" }
  }
  </script>

  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-K196Y4ZCFK"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-K196Y4ZCFK');
  </script>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="style.css"/>

  <style>
    .raw-value {
      font-size: 2.8rem;
      font-weight: 700;
      line-height: 1.1;
    }
    .raw-unit {
      font-size: 1rem;
      color: #6c757d;
    }
    @media (max-width: 768px) {
      .raw-value { font-size: 1.8rem; }
      .raw-unit { font-size: .85rem; }
    }
    .plan-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .plan-list li {
      padding: 6px 0;
      font-size: 1.05rem;
      border-bottom: 1px solid #f0f0f0;
    }
    .plan-list li:last-child {
      border-bottom: none;
    }
    .plan-list li:has(+ .plan-totals-row) {
      border-bottom: none;
    }
    .plan-totals-row + li {
      padding-top: 14px;
    }
    .plan-item-qty {
      font-weight: 700;
      font-size: 1.2rem;
    }
    .stops-big {
      font-size: 3rem;
      font-weight: 700;
      line-height: 1;
    }
    .slider-label-desc {
      font-size: 0.8rem;
      color: #6c757d;
      display: block;
      margin-top: 2px;
    }
    .carbs-label {
      font-weight: 600;
      color: #0d6efd;
    }
    .plan-section-divider {
      font-size: 0.7rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: .08em;
      color: #adb5bd;
      padding: 8px 0 2px;
      border-bottom: none !important;
    }
    .plan-totals-row {
      background: whitesmoke;
      margin: 6px -12px -8px;
      padding: 10px 12px !important;
      border-top: 2px solid lightgray !important;
      border-bottom: none !important;
      border-radius: 0 0 6px 6px;
    }
    .totals-heading {
      display: block;
      font-size: 0.7rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: gray;
      margin-bottom: 8px;
    }
    .totals-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }
    .totals-chip {
      display: flex;
      flex-direction: column;
      background: white;
      border: 1px solid lightgray;
      border-radius: 6px;
      padding: 6px 10px;
      min-width: 72px;
    }
    .totals-chip-value {
      font-size: 1rem;
      font-weight: 700;
      line-height: 1.2;
    }
    .totals-chip-label {
      font-size: 0.7rem;
      color: gray;
    }
    .plan-alt-divider {
      font-size: 0.75rem;
      font-style: italic;
      color: #adb5bd;
      padding: 2px 0;
      border-bottom: none !important;
      text-align: center;
    }
    .bottle-recipe {
      border-left: 3px solid #0d6efd;
      border-radius: 0 6px 6px 0;
      padding: 6px 10px;
    }
    .bottle-recipe__label {
      font-size: 0.7rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: .06em;
      color: #0d6efd;
      margin-bottom: 5px;
    }
    .bottle-recipe__chips {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
    }
    .recipe-chip {
      display: inline-flex;
      align-items: baseline;
      gap: 3px;
      font-size: 1rem;
      font-weight: 700;
      padding: 3px 10px;
      border-radius: 20px;
    }
    .recipe-chip__label {
      font-size: 0.7rem;
      font-weight: 500;
      opacity: 0.85;
    }
    .recipe-chip--malto    { background: #dbeafe; color: #1d4ed8; }
    .recipe-chip--fructose { background: #fed7aa; color: #7c2d12; }
    .recipe-chip--citrate  { background: #d1fae5; color: #065f46; }
  </style>
</head>

<body>
  <nav role="navigation" class="navbar">
    <a href="https://racefeed.com.br/" class="logo">RaceFeed</a>
    <button id="nav-toggle" aria-label="Menu" aria-expanded="false">&#9776;</button>
    <ul class="nav-list" id="nav-list">
      <li><a href="index.html">Calendar</a></li>
      <li><a href="gearftp.html">Gear vs FTP</a></li>
      <li><a href="fuelplan.html">Fuel Plan</a></li>
      <li><a href="https://forms.gle/oo9s7hdqg29Wznkz7">Newsletter</a></li>
    </ul>
  </nav>

  <main role="main" class="content">
  <div class="container py-4">

    <!-- Parameters Card -->
    <div class="card mb-4">
      <div class="card-header d-flex align-items-center justify-content-between">
        <span class="fw-semibold">🎯 Ride Parameters</span>
        <button id="shareBtn" class="btn btn-sm btn-outline-secondary" onclick="share()">🔗 Share</button>
      </div>
      <div class="card-body">
        <div class="row g-4">

          <!-- Left column -->
          <div class="col-md-6 d-flex flex-column gap-4">

            <div>
              <label class="form-label">
                🍌 Carbs target: <strong><span id="carbsValue">40</span> g/h</strong>
                — <span class="carbs-label" id="carbsLabel">Moderate</span>
              </label>
              <input type="range" class="form-range" id="carbsSlider" min="20" max="120" step="10" value="40">
              <span class="slider-label-desc" id="carbsHint">Suitable for most athletes in races over 2h</span>
            </div>

            <div>
              <label class="form-label">
                🏋️ Body weight: <strong><span id="weightValue">70</span> kg</strong>
              </label>
              <input type="range" class="form-range" id="weightSlider" min="40" max="120" step="1" value="70">
            </div>

            <div>
              <label class="form-label">
                🧂 Sweat saltiness: <strong><span id="saltinessValue">700</span> mg/L</strong>
                — <span id="saltinessLabel">Average — Most athletes</span>
              </label>
              <input type="range" class="form-range" id="saltinessSlider" min="300" max="1600" step="100" value="700">
              <span class="slider-label-desc" id="saltinessHint">Look for white marks on skin or kit after a ride — that's a sign of salty sweat.</span>
            </div>

            <div>
              <label class="form-label">
                💧 Dehydration tolerance: <strong><span id="lossValue">1.0</span>% BW</strong>
                — <span id="lossLabel">Optimal</span>
              </label>
              <input type="range" class="form-range" id="lossSlider" min="0" max="3" step="0.5" value="1">
              <span class="slider-label-desc" id="lossHint">ACSM recommendation: no performance impact</span>
            </div>

          </div>

          <!-- Right column -->
          <div class="col-md-6 d-flex flex-column gap-4">

            <div>
              <label class="form-label">
                🔥 Intensity: <strong><span id="zoneValue">Z3 — Tempo</span></strong>
              </label>
              <input type="range" class="form-range" id="zoneSlider" min="1" max="5" step="1" value="3">
              <span class="slider-label-desc" id="zoneHint">Sustained effort, group ride pace</span>
            </div>

            <div>
              <label class="form-label">
                🌡️ Temperature: <strong><span id="tempValue">25</span>°C</strong>
                — <span id="tempLabel">Warm</span>
              </label>
              <input type="range" class="form-range" id="tempSlider" min="0" max="40" step="1" value="25">
            </div>

            <div>
              <label class="form-label">
                ⏱️ Duration (Moving Time): <strong><span id="durationValue">3</span>h</strong>
              </label>
              <input type="range" class="form-range" id="durationSlider" min="1" max="16" step="0.5" value="3">
              <div id="durationWarning" class="alert alert-warning py-1 px-2 mt-2 small mb-0" style="display:none">
                ⚠️ <strong>Note:</strong> What your gut tolerates in 2–3h is completely different from what it tolerates in 4–6h+. Train your nutrition strategy well before race day.
              </div>
              <div id="durationZoneWarning" class="alert alert-danger py-1 px-2 mt-2 small mb-0" style="display:none">
                ⚠️ <strong>Note:</strong> Unrealistic expectations — Z4/Z5 efforts are rarely sustainable beyond 60–90 min.
              </div>
            </div>

            <div class="mt-auto">
              <p class="text-muted small mb-0" id="summaryText"></p>
            </div>

          </div>

        </div>
      </div>
    </div>

    <!-- Requirements Card -->
    <div class="card mb-4"> 
      <div class="card-header d-flex align-items-center justify-content-between">
        <span class="fw-semibold">📊 Requirements <span class="badge bg-secondary fw-normal" style="font-size:0.65rem">Beta</span></span>

        <button id="shareBtn2" class="btn btn-sm btn-outline-secondary" onclick="share()">🔗 Share</button>
      </div>
      <div class="card-body p-0">
        <div class="row g-0 text-center align-items-stretch">
          <div class="col-6 col-md-3 d-flex flex-column align-items-center py-3 px-2">
            <div class="raw-value text-success" id="carbsOut">—</div>
            <div class="raw-unit">g of carbs</div>
            <div class="text-muted mt-auto pt-1" style="font-size:.75rem" id="carbsRateOut"></div>
          </div>
          <div class="col-6 col-md-3 d-flex flex-column align-items-center py-3 px-2">
            <div class="raw-value" style="color:#c87000;" id="sodiumOut">—</div>
            <div class="raw-unit">sodium lost (Na)</div>
            <div class="text-muted mt-auto pt-1" style="font-size:.75rem" id="sodiumRateOut"></div>
          </div>
          <div class="col-6 col-md-3 d-flex flex-column align-items-center py-3 px-2">
            <div class="raw-value" style="color:#7a3fa0;" id="potassiumOut">—</div>
            <div class="raw-unit">potassium lost (K)</div>
            <div class="text-muted mt-auto pt-1" style="font-size:.75rem" id="potassiumCoverageOut">via sweat — mainly from food</div>
          </div>
          <div class="col-6 col-md-3 d-flex flex-column align-items-center py-3 px-2">
            <div class="raw-value text-primary" id="waterOut">—</div>
            <div class="raw-unit">L of water</div>
            <div class="text-muted mt-auto pt-1" style="font-size:.75rem" id="sweatOut"></div>
          </div>
        </div>
      </div>
    </div>

    <div class="row mb-4">
      <div class="col-md-8 mb-3 mb-md-0">
        <div class="card h-100">
          <div class="card-header d-flex align-items-center justify-content-between flex-wrap gap-2">
            <span class="fw-semibold">🍽️ Nutrition Plan</span>
            <div class="btn-group btn-group-sm" role="group">
              <button type="button" class="btn btn-primary" id="btnPeba"    onclick="setMode('peba')">🐢 Peba</button>
              <button type="button" class="btn btn-outline-primary" id="btnBruto"    onclick="setMode('bruto')">🐴 Bruto</button>
              <button type="button" class="btn btn-outline-primary" id="btnGalatico" onclick="setMode('galatico')">👽 Galático</button>
            </div>
          </div>
          <div class="card-body">
            <ul class="plan-list" id="planList"></ul>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card h-100">
          <div class="card-header fw-semibold">🚰 Bottles & Hydration</div>
          <div class="card-body">
            <div class="mb-3">
              <label class="form-label mb-1">
                🍶 Bottles: <strong><span id="bottlesValue">1.50</span> L</strong>
              </label>
              <input type="range" class="form-range mb-1" id="bottlesSlider" min="500" max="3000" step="250" value="1500">
              <span class="slider-label-desc" id="bottlesLabel">2 × 750 ml</span>
            </div>
            <div class="mb-3">
              <label class="form-label mb-1">
                🎒 Hydration pack: <strong><span id="packValue">—</span> L</strong>
              </label>
              <input type="range" class="form-range mb-1" id="packSlider" min="0" max="3000" step="500" value="0">
              <span class="slider-label-desc" id="packLabel">None</span>
            </div>
            <div id="stopsBody"></div>
          </div>
        </div>
      </div>
    </div>

  </div>
  </main>

  <footer role="contentinfo" class="footer">
    <div class="footer-content">
      <p>&copy; 2025 RaceFeed. Todos os direitos reservados.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    // ── Lookup tables ────────────────────────────────────────────────

    const CARBS_LABELS = {
      20:  { label: 'Minimal',     hint: 'Recovery or very short ride (< 45 min) — pure glucose' },
      30:  { label: 'Light',       hint: 'Easy Z1–Z2, 45–75 min — pure glucose' },
      40:  { label: 'Moderate',    hint: 'Aerobic base — pure glucose, well below SGLT1 limit' },
      50:  { label: 'Moderate',    hint: 'Endurance pace — pure glucose, approaching SGLT1 limit' },
      60:  { label: 'High',        hint: 'SGLT1 ceiling (~60 g/h) — add fructose to go beyond' },
      70:  { label: 'High',        hint: '~60g glucose + 10g fructose — dual-transporter begins' },
      80:  { label: 'Advanced',    hint: '~60g glucose + 20g fructose (3:1) — gut adaptation helps' },
      90:  { label: 'Advanced',    hint: '~60g glucose + 30g fructose (2:1) — Jeukendrup ceiling' },
      100: { label: 'Elite',       hint: '~60g glucose + 40g fructose (3:2) — gut training required' },
      110: { label: 'Elite',       hint: '~60g glucose + 50g fructose (~1:0.8) — extreme gut adaptation' },
      120: { label: 'Elite',       hint: '~60g glucose + 60g fructose (1:1) — Experimental' },
    };

    function tempLabel(t) {
      if (t <= 12) return 'Cold';
      if (t <= 18) return 'Cool';
      if (t <= 23) return 'Mild';
      if (t <= 28) return 'Warm';
      if (t <= 33) return 'Hot';
      if (t <= 38) return 'Very hot';
      return 'Extreme heat';
    }

    const BOTTLES_LABELS = {
       500: '1 × 500 ml',
       750: '1 × 750 ml',
      1000: '2 × 500 ml',
      1250: '1 × 750 ml + 1 × 500 ml',
      1500: '2 × 750 ml',
      1750: '2 × 750 ml + 1 × 250 ml',
      2000: '2 × 1000 ml',
      2250: '3 × 750 ml',
      2500: '2 × 1000 ml + 1 × 500 ml',
      2750: '2 × 1000 ml + 1 × 750 ml',
      3000: '3 × 1000 ml',
    };

    // Sweat sodium concentration breakpoints (mg/L).
    // Range 300–1600 mg/L covers the vast majority of athletes.
    //   [A] Precision Hydration — sweat test database: ~500 (low) / ~1000 (moderate) / ~1500 (high)
    //       https://www.precisionhydration.com/performance-advice/hydration/how-to-estimate-sweat-salt-loss/
    //   [B] PMC5371639 — Baker et al. (2017): population range 230–2300 mg/L,
    //       most athletes 500–1300 mg/L, mean ~900–950 mg/L.
    //       https://pmc.ncbi.nlm.nih.gov/articles/PMC5371639/
    function saltinessLabel(v) {
      if (v <= 400)  return 'Low — Light sweater';       // <500: bottom ~20% per [A]
      if (v <= 600)  return 'Less than average';
      if (v <= 1000) return 'Average — Most athletes';   // ~900 mg/L mean per [B]
      if (v <= 1400) return 'More than average';
      return 'High — Salty sweater';                     // >1400: top ~10% per [A]
    }

    // ── Formulas ─────────────────────────────────────────────────────

    // Base sweat rates at 20°C, 70 kg, by training zone (L/h).
    // Zone boundaries follow the 5-zone model (British Cycling / Coggan).
    // Values calibrated from:
    //   [1] Goulet (2011) — Effect of exercise-induced dehydration on endurance performance:
    //       meta-analysis shows sweat rates 0.5–2.5 L/h across intensities and temperatures.
    //       https://pubmed.ncbi.nlm.nih.gov/21207272/
    //   [2] PMC8226773 — Sweat rate in endurance athletes:
    //       mean 0.64 L/h (<10°C), 0.69 L/h (10–20°C), 1.08 L/h (>20°C) at mixed intensities.
    //       https://pmc.ncbi.nlm.nih.gov/articles/PMC8226773/
    //   [3] Sawka et al. ACSM 2007 Position Stand — typical cycling sweat rates 0.5–1.8 L/h
    //       depending on intensity and environment.
    //       https://pubmed.ncbi.nlm.nih.gov/17277604/
    const SWEAT_BASE = [0, 0.70, 0.85, 1.00, 1.15, 1.30]; // L/h, zones 1–5

    // Sweat potassium concentration: ~160 mg/L (range 120–220 mg/L).
    // Individual variation is smaller than sodium, so a fixed coefficient is reasonable.
    //   PMC5371639 — Baker et al. (2017): sweat K+ concentration data.
    const SWEAT_K = 160; // mg/L

    function calcSweatRate(zone, temp, weight) {
      const base = SWEAT_BASE[zone];

      // Temperature effect on sweat rate.
      // Below 20°C: mild suppression (~0.008 L/h per °C).
      // Above 20°C: strong increase (~0.035 L/h per °C).
      // Coefficients derived from:
      //   [2] PMC8226773 — ~0.40 L/h difference between <10°C and >20°C bands,
      //       implying ≈0.03–0.04 L/h/°C above the 20°C threshold.
      //   [4] Montain & Coyle (1992) — fluid ingestion during exercise:
      //       heat stress linearly increases sweat rate above thermoneutral (~20°C).
      //       https://pubmed.ncbi.nlm.nih.gov/1447078/
      const temp_delta = temp < 20
        ? (temp - 20) * 0.008   // cold: ~−0.16 L/h at 0°C
        : (temp - 20) * 0.035;  // hot:  ~+0.70 L/h at 40°C

      // Linear weight scaling: heavier athletes produce proportionally more heat
      // and therefore more sweat. Normalised to 70 kg reference body mass.
      //   [3] Sawka et al. ACSM 2007 — body mass is the dominant individual factor
      //       in absolute sweat rate; https://pubmed.ncbi.nlm.nih.gov/17277604/
      return Math.min(2.5, Math.max(0.2, (base + temp_delta) * (weight / 70)));
    }

    // Intensity zone labels
    const ZONE_LABELS = {
      1: { label: 'Z1 — Active Recovery', hint: 'Easy spin, recovery ride, very low effort' },
      2: { label: 'Z2 — Endurance',       hint: 'Comfortable aerobic pace, long rides' },
      3: { label: 'Z3 — Tempo',           hint: 'Sustained effort, group ride pace' },
      4: { label: 'Z4 — Threshold / Race',hint: 'Race effort, ~1h max sustainable power' },
      5: { label: 'Z5 — VO2max',          hint: 'Very hard efforts, intervals, short races' },
    };

    // Dehydration tolerance labels.
    // Thresholds from:
    //   [3] Sawka et al. ACSM 2007 — ≥2% BW loss impairs aerobic performance,
    //       especially in heat; <1% has negligible effect on most athletes.
    //       https://pubmed.ncbi.nlm.nih.gov/17277604/
    //   [5] Jeukendrup & Gleeson (2010) — Sport Nutrition, 2nd ed.:
    //       practical target is 1–2% BW loss for endurance events.
    function lossLabelFn(v) {
      if (v === 0)   return { label: 'Full replacement', hint: 'Drink everything you sweat — hard to achieve in practice' };
      if (v <= 1.0)  return { label: 'Optimal',          hint: 'ACSM rec.: <1% loss has no effect on performance [3]' };
      if (v <= 2.0)  return { label: 'Moderate deficit', hint: 'Acceptable limit — performance degrades above 2% in heat [3]' };
      return             { label: 'High deficit',     hint: 'Only in cool conditions (<20°C) — significant impairment risk [3]' };
    }

    function calculate() {
      const duration   = parseFloat(document.getElementById('durationSlider').value);
      const carbsph    = parseInt(document.getElementById('carbsSlider').value);
      const weight     = parseFloat(document.getElementById('weightSlider').value);
      const temp       = parseFloat(document.getElementById('tempSlider').value);
      const bottles_ml = parseInt(document.getElementById('bottlesSlider').value);
      const pack_ml    = parseInt(document.getElementById('packSlider').value);
      const capacity   = bottles_ml + pack_ml;
      const saltiness  = parseInt(document.getElementById('saltinessSlider').value);
      const zone       = parseInt(document.getElementById('zoneSlider').value);
      const lossPercent = parseFloat(document.getElementById('lossSlider').value);

      const sweat       = calcSweatRate(zone, temp, weight);
      const total_sweat = sweat * duration;
      const loss_allowance = weight * (lossPercent / 100); // kg ≈ L
      const water       = Math.max(0, total_sweat - loss_allowance);
      const sodium      = total_sweat * saltiness; // sodium lost via sweat (not reduced by deficit)
      const potassium   = total_sweat * SWEAT_K;  // K+ lost via sweat — PMC5371639
      const carbs       = carbsph * duration;

      return { duration, carbsph, weight, temp, capacity, bottles_ml, pack_ml, saltiness, zone, lossPercent,
               sweat, total_sweat, water, sodium, potassium, carbs };
    }

    // ── Food database ────────────────────────────────────────────────

    // Potassium sources: USDA FoodData Central, TACO, product labels
    const FOODS = {
      gel:      { label: 'carbo gel',                    carbs: 22, sodium:  55, potassium:  20, water:   0 },
      gatorade: { label: 'Gatorade (500 ml)',             carbs: 29, sodium: 253, potassium:  75, water: 500 },
      coca:     { label: 'Coca-Cola (350 ml)',            carbs: 37, sodium:  45, potassium:  28, water: 350 },
      banana:   { label: 'banana',                        carbs: 22, sodium:  15, potassium: 422, water:  75 },
      pacoca:   { label: 'Paçoquinha',                   carbs: 10, sodium:  37, potassium: 120, water:   0 },
      doceleite: { label: 'doce de leite (30 g)',          carbs: 15, sodium:  80, potassium: 110, water:   0 },
      // Electrolyte supplements — all per 1 g. Na computed from molecular weight; K from Morton label.
      saltcap:  { label: 'salt capsule',       carbs: 0, sodium: 300, potassium:   0, water: 0 }, // typical sports capsule (SaltStick, Precision Hydration etc.)
      salt:     { label: 'table salt',         carbs: 0, sodium: 393, potassium:   0, water: 0 }, // NaCl: Na/MW = 23/58.4 = 39.4%
      bicarb:   { label: 'sodium bicarbonate', carbs: 0, sodium: 274, potassium:   0, water: 0 }, // NaHCO₃: Na/MW = 23/84 = 27.4%
      litesalt: { label: 'lite salt',          carbs: 0, sodium: 207, potassium: 250, water: 0 }, // Morton 50% NaCl + 50% KCl
      nacitrate:{ label: 'sodium citrate',     carbs: 0, sodium: 267, potassium:   0, water: 0 }, // Na₃C₆H₅O₇: 3Na/MW = 69/258 = 26.7%
      // Carb powders — per 1 g (used in Galatico plan)
      malto:    { label: 'maltodextrin',       carbs: 1, sodium:   0, potassium:   0, water: 0 }, // glucose polymer, absorbed via SGLT1
      fructose: { label: 'fructose',           carbs: 1, sodium:   0, potassium:   0, water: 0 }, // absorbed via GLUT5
    };

    // ── Nutrition Plan ───────────────────────────────────────────────

    let planMode = 'peba';

    function setMode(mode) {
      planMode = mode;
      document.getElementById('btnPeba').className     = mode === 'peba'     ? 'btn btn-sm btn-primary' : 'btn btn-sm btn-outline-primary';
      document.getElementById('btnBruto').className    = mode === 'bruto'    ? 'btn btn-sm btn-primary' : 'btn btn-sm btn-outline-primary';
      document.getElementById('btnGalatico').className = mode === 'galatico' ? 'btn btn-sm btn-primary' : 'btn btn-sm btn-outline-primary';
      update();
    }

    function buildPlan(water_L, carbs_g, sodium_mg, potassium_mg, duration_h, weight) {
      if (duration_h <= 1)
        return { items: [{ text: 'Water only — no carbs or sodium needed for rides under 1 hour.' }], potassium_covered: 0, potassium_target: potassium_mg };
      if (planMode === 'galatico') return buildPlanGalatico(water_L, carbs_g, sodium_mg, potassium_mg, weight, duration_h);
      if (planMode === 'bruto')    return buildPlanBruto(water_L, carbs_g, sodium_mg, potassium_mg, duration_h);
      return buildPlanPeba(water_L, carbs_g, sodium_mg, potassium_mg, duration_h);
    }

    // 🐢 Peba — real foods + Gatorade
    // Strategy: Gatorade for hydration → Coke for a carb burst → bananas → paçoca → salt top-up → plain water.
    // rem_water is clamped to 0 after each food step so later steps never operate on a negative base.
    // rem_sodium tracks the running deficit (positive = still needed, negative = already covered by food).
    function buildPlanPeba(water_L, carbs_g, sodium_mg, potassium_mg, duration_h) {
      let rem_carbs  = carbs_g;
      let rem_sodium = sodium_mg;
      let rem_water  = water_L;

      // 1. Gatorade: cover 60% of water needs, capped at 1 bottle per 2h
      const maxGatorades   = Math.floor(duration_h / 2) + 1; // 1 at the start + 1 more every 2h
      const gatoradeTarget = Math.round((rem_water * 0.6 * 1000) / FOODS.gatorade.water);
      const gatorades      = Math.min(maxGatorades, Math.max(0, gatoradeTarget));
      rem_carbs  -= gatorades * FOODS.gatorade.carbs;
      rem_sodium -= gatorades * FOODS.gatorade.sodium;
      rem_water   = Math.max(0, rem_water - gatorades * (FOODS.gatorade.water / 1000));

      // 2. Coca-Cola: cover 30% of remaining carbs (carb-dense + caffeinated — late-ride boost)
      rem_carbs        = Math.max(0, rem_carbs); // clamp before percentage split
      const colaTarget = Math.round((rem_carbs * 0.3) / FOODS.coca.carbs);
      const cocas      = Math.max(0, colaTarget);
      rem_carbs  = Math.max(0, rem_carbs  - cocas * FOODS.coca.carbs);
      rem_sodium -= cocas * FOODS.coca.sodium;
      rem_water   = Math.max(0, rem_water - cocas * (FOODS.coca.water / 1000));

      // 3. Banana: cover 40% of remaining carbs (also contributes a small amount of water)
      const bananaTarget = Math.round((rem_carbs * 0.4) / FOODS.banana.carbs);
      const bananas      = Math.max(0, bananaTarget);
      rem_carbs  = Math.max(0, rem_carbs  - bananas * FOODS.banana.carbs);
      rem_sodium -= bananas * FOODS.banana.sodium;
      rem_water   = Math.max(0, rem_water - bananas * (FOODS.banana.water / 1000));

      // 4. Paçoquinha: cover all remaining carbs (rounds up to not leave a deficit)
      const pacocas = Math.max(0, Math.ceil(rem_carbs / FOODS.pacoca.carbs));
      rem_sodium -= pacocas * FOODS.pacoca.sodium;

      // 5. Extra salt: cover remaining sodium deficit
      const sodium_deficit_mg = Math.max(0, rem_sodium);
      const salt_g = sodium_deficit_mg > 0
        ? Math.ceil((sodium_deficit_mg / FOODS.salt.sodium) * 10) / 10  // round up to 0.1 g
        : 0;

      // 6. Plain water: whatever hydration remains after all food/drinks
      const plain_w = rem_water; // already clamped at each step above

      const potassium_covered = gatorades * FOODS.gatorade.potassium
                               + cocas    * FOODS.coca.potassium
                               + bananas  * FOODS.banana.potassium
                               + pacocas  * FOODS.pacoca.potassium;

      const totals = {
        carbs:     gatorades * FOODS.gatorade.carbs + cocas * FOODS.coca.carbs + bananas * FOODS.banana.carbs + pacocas * FOODS.pacoca.carbs,
        water_ml:  gatorades * FOODS.gatorade.water + cocas * FOODS.coca.water + bananas * FOODS.banana.water + Math.round(plain_w * 1000),
        sodium:    gatorades * FOODS.gatorade.sodium + cocas * FOODS.coca.sodium + bananas * FOODS.banana.sodium + pacocas * FOODS.pacoca.sodium + salt_g * FOODS.salt.sodium,
        potassium: potassium_covered,
      };

      const hydroItems = [];
      if (gatorades > 0)
        hydroItems.push({ qty: gatorades, unit: FOODS.gatorade.label + (gatorades > 1 ? 's' : ''), details: nutriDetails(gatorades, FOODS.gatorade) });
      if (salt_g > 0)
        hydroItems.push({ qty: salt_g.toFixed(1) + ' g', unit: FOODS.salt.label, details: `${Math.round(salt_g * FOODS.salt.sodium)} mg Na` });
      if (plain_w >= 0.1)
        hydroItems.push({ qty: plain_w.toFixed(1) + ' L', unit: 'plain water', details: `${(plain_w * 1000).toFixed(0)} ml` });

      const fuelItems = [];
      if (cocas > 0)
        fuelItems.push({ qty: cocas,   unit: FOODS.coca.label,                               details: nutriDetails(cocas, FOODS.coca) });
      if (bananas > 0)
        fuelItems.push({ qty: bananas, unit: FOODS.banana.label + (bananas > 1 ? 's' : ''), details: nutriDetails(bananas, FOODS.banana) });
      if (pacocas > 0)
        fuelItems.push({ qty: pacocas, unit: FOODS.pacoca.label + (pacocas > 1 ? 's' : ''), details: nutriDetails(pacocas, FOODS.pacoca) });

      const items = [];
      if (hydroItems.length) items.push({ divider: '💧 Hydration & Electrolytes' }, ...hydroItems);
      if (fuelItems.length)  items.push({ divider: '🍌 Fuel' }, ...fuelItems);
      items.push({ totals });
      items.push({ note: 'Space it out every 20–30 min for best absorption.' });
      return { items, potassium_covered, potassium_target: potassium_mg };
    }

    // 🐴 Bruto — gels + doce de leite for carbs, Gatorade for hydration, salt capsules for sodium.
    // Strategy: max out gels (fast absorption, 2/h limit) → doce de leite for leftover carbs →
    //           Gatorade for half the water → salt capsules to top up sodium → plain water for the rest.
    // Both gels and doce de leite round down (Math.floor) so total carbs ≤ carbs_g always holds.
    function buildPlanBruto(water_L, carbs_g, sodium_mg, potassium_mg, duration_h) {
      // 1. Gels: cover 70% of carbs, hard cap at 2 gels per hour
      const maxGels   = Math.floor(duration_h * 2);        // e.g. 3h → max 6 gels
      const gelTarget = Math.floor((carbs_g * 0.7) / FOODS.gel.carbs);
      const gels      = Math.min(maxGels, Math.max(0, gelTarget));
      let rem_carbs  = Math.max(0, carbs_g  - gels * FOODS.gel.carbs);  // clamp: gels may overshoot by <1 gel worth
      let rem_sodium = sodium_mg - gels * FOODS.gel.sodium;

      // 2. Doce de leite: cover remaining carbs.
      // Math.floor — never exceed the target. Both gel and doce round down, so total ≤ carbs_g.
      // Max undershot is <1 doce pack (15 g), which is acceptable discretization.
      const doces = Math.max(0, Math.floor(rem_carbs / FOODS.doceleite.carbs));
      rem_sodium -= doces * FOODS.doceleite.sodium;

      // 3. Gatorade: cover 50% of water, capped at 1 bottle per 2h
      const maxGatorades    = Math.floor(duration_h / 2) + 1; // 1 at start + 1 more every 2h
      const gatoradeTarget  = Math.round((water_L * 0.5 * 1000) / FOODS.gatorade.water);
      const gatorades       = Math.min(maxGatorades, Math.max(0, gatoradeTarget));
      rem_sodium -= gatorades * FOODS.gatorade.sodium;
      const rem_water = Math.max(0, water_L - gatorades * (FOODS.gatorade.water / 1000));

      // 4. Salt capsules: cover remaining sodium deficit
      const sodium_deficit_mg = Math.max(0, rem_sodium);
      const saltcaps = sodium_deficit_mg > 0
        ? Math.ceil(sodium_deficit_mg / FOODS.saltcap.sodium)
        : 0;

      // 5. Plain water: remaining hydration after Gatorade
      const plain_w = rem_water; // already clamped above

      const potassium_covered = gels      * FOODS.gel.potassium
                               + doces     * FOODS.doceleite.potassium
                               + gatorades * FOODS.gatorade.potassium;

      const totals = {
        carbs:     gels * FOODS.gel.carbs + doces * FOODS.doceleite.carbs,
        water_ml:  gatorades * FOODS.gatorade.water + Math.round(plain_w * 1000),
        sodium:    gels * FOODS.gel.sodium + doces * FOODS.doceleite.sodium + gatorades * FOODS.gatorade.sodium + saltcaps * FOODS.saltcap.sodium,
        potassium: potassium_covered,
      };

      const hydroItems = [];
      if (gatorades > 0)
        hydroItems.push({ qty: gatorades, unit: FOODS.gatorade.label + (gatorades > 1 ? 's' : ''), details: nutriDetails(gatorades, FOODS.gatorade) });
      if (saltcaps > 0)
        hydroItems.push({ qty: saltcaps, unit: FOODS.saltcap.label + (saltcaps > 1 ? 's' : ''), details: `${saltcaps * FOODS.saltcap.sodium} mg Na` });
      if (plain_w >= 0.1)
        hydroItems.push({ qty: plain_w.toFixed(1) + ' L', unit: 'plain water', details: `${(plain_w * 1000).toFixed(0)} ml` });

      const fuelItems = [];
      if (gels > 0)
        fuelItems.push({ qty: gels,  unit: FOODS.gel.label + (gels > 1 ? 's' : ''),  details: nutriDetails(gels, FOODS.gel) });
      if (doces > 0)
        fuelItems.push({ qty: doces, unit: FOODS.doceleite.label,                      details: nutriDetails(doces, FOODS.doceleite) });

      const items = [];
      if (hydroItems.length) items.push({ divider: '💧 Hydration & Electrolytes' }, ...hydroItems);
      if (fuelItems.length)  items.push({ divider: '🍌 Fuel' }, ...fuelItems);
      items.push({ totals });
      items.push({ note: 'Take a gel every 20–30 min. Wash down with water.' });
      return { items, potassium_covered, potassium_target: potassium_mg };
    }

    // 🚀 Galático — gels + sodium citrate (in-ride) + bicarb pre-load, max performance
    function buildPlanGalatico(water_L, carbs_g, sodium_mg, potassium_mg, weight, duration_h) {
      // Pre-ride: bicarb 3-bolus — 3 × 0.1 g/kg every 30 min, starting 90 min before.
      // Total = 0.3 g/kg. Splitting reduces GI distress vs single dose (ISSN 2021 — PMC8427947).
      const bicarb_bolus_g = Math.round(weight * 0.1);
      const bicarb_total_g = bicarb_bolus_g * 3;

      const showBicarb = duration_h <= 2;
      const bicarb_sodium = showBicarb ? bicarb_total_g * FOODS.bicarb.sodium : 0;

      // 1. Carbs: glucose fixed at SGLT1 ceiling (60 g/h), fructose on top (Jeukendrup)
      const glucose_g  = Math.min(carbs_g, 60 * duration_h);
      const fructose_g = Math.max(0, carbs_g - glucose_g);

      // 2. Sodium citrate: cover full sweat deficit minus bicarb contribution
      const rem_sodium = Math.max(0, sodium_mg - bicarb_sodium);
      const citrate_g = rem_sodium > 0
        ? Math.ceil((rem_sodium / FOODS.nacitrate.sodium) * 10) / 10
        : 0;

      // 3. Carbs + citrate dissolved in all water (no plain water — everything goes in the bottle)
      const plain_w = water_L;

      const potassium_covered = 0; // no food sources in Galatico

      const totals = {
        carbs:    glucose_g + fructose_g,
        water_ml: Math.round(plain_w * 1000),
        sodium:   citrate_g * FOODS.nacitrate.sodium + bicarb_sodium,
        potassium: 0,
      };

      const hydroItems = [];
      if (citrate_g > 0)
        hydroItems.push({ qty: citrate_g.toFixed(1) + ' g', unit: FOODS.nacitrate.label, details: `${Math.round(citrate_g * FOODS.nacitrate.sodium)} mg Na` });
      if (plain_w >= 0.1)
        hydroItems.push({ qty: plain_w.toFixed(1) + ' L', unit: 'water', details: `dissolve all powders in your bottles` });

      const fuelItems = [];
      fuelItems.push({ qty: Math.round(glucose_g) + ' g', unit: FOODS.malto.label, details: `${Math.round(glucose_g)} g carbs · SGLT1 (glucose transporter)` });
      if (fructose_g >= 1)
        fuelItems.push({ qty: Math.round(fructose_g) + ' g', unit: FOODS.fructose.label, details: `${Math.round(fructose_g)} g carbs · GLUT5 (fructose transporter)` });

      const items = [];
      if (showBicarb) {
        items.push(
          { divider: '⏱️ Pre-ride' },
          { qty: `3 × ${bicarb_bolus_g} g`, unit: FOODS.bicarb.label, details: `every 30 min · start 90 min before · total ${bicarb_total_g} g · ${Math.round(bicarb_sodium)} mg Na` }
        );
      }
      if (hydroItems.length) items.push({ divider: '💧 Hydration & Electrolytes' }, ...hydroItems);
      if (fuelItems.length)  items.push({ divider: '🍌 Fuel' }, ...fuelItems);
      items.push({ totals });
      items.push({ note: 'Dissolve malto, fructose, and citrate in your bottles. Bicarb with food/water pre-ride. Drink consistently from km 0.' });
      return { items, potassium_covered, potassium_target: potassium_mg, citrate_g, glucose_g, fructose_g };
    }

    function nutriDetails(qty, food) {
      const parts = [];
      if (food.carbs     > 0) parts.push(`${food.carbs     * qty} g carbs`);
      if (food.water     > 0) parts.push(`${(food.water     * qty / 1000).toFixed(1)} L water`);
      if (food.sodium    > 0) parts.push(`${food.sodium    * qty} mg Na`);
      if (food.potassium > 0) parts.push(`${food.potassium * qty} mg K`);
      return parts.join(' · ');
    }

    // ── Water Stops ──────────────────────────────────────────────────

    function buildStops(water_L, capacity_ml, duration_h) {
      const cap = capacity_ml / 1000;
      const stops = Math.ceil(water_L / cap) - 1; // start full, finish line excluded
      if (stops <= 0) return null;
      const interval_min = Math.round((duration_h / (stops + 1)) * 60);
      return { stops, interval_min };
    }

    // ── Render ───────────────────────────────────────────────────────

    function update() {
      const r = calculate();

      // Slider labels
      document.getElementById('durationValue').textContent = r.duration;
      document.getElementById('durationWarning').style.display     = r.duration >= 4 ? '' : 'none';
      document.getElementById('durationZoneWarning').style.display = (r.zone >= 4 && r.duration > 1.5) ? '' : 'none';
      document.getElementById('carbsValue').textContent    = r.carbsph;
      document.getElementById('weightValue').textContent   = r.weight;
      document.getElementById('tempValue').textContent     = r.temp;
      document.getElementById('bottlesValue').textContent = (r.bottles_ml / 1000).toFixed(2);
      document.getElementById('packValue').textContent    = r.pack_ml === 0 ? '—' : (r.pack_ml / 1000).toFixed(2);
      document.getElementById('saltinessValue').textContent = r.saltiness;
      document.getElementById('lossValue').textContent      = r.lossPercent.toFixed(1);

      const cl = CARBS_LABELS[r.carbsph];
      document.getElementById('carbsLabel').textContent    = cl.label;
      document.getElementById('carbsHint').textContent     = cl.hint;
      document.getElementById('tempLabel').textContent     = tempLabel(r.temp);
      document.getElementById('bottlesLabel').textContent = BOTTLES_LABELS[r.bottles_ml] || (r.bottles_ml / 1000).toFixed(2) + ' L';
      document.getElementById('packLabel').textContent    = r.pack_ml === 0 ? 'None' : (r.pack_ml / 1000).toFixed(2) + ' L pack';
      document.getElementById('saltinessLabel').textContent = saltinessLabel(r.saltiness);

      const zl = ZONE_LABELS[r.zone];
      document.getElementById('zoneValue').textContent = zl.label;
      document.getElementById('zoneHint').textContent  = zl.hint;

      const ll = lossLabelFn(r.lossPercent);
      document.getElementById('lossLabel').textContent = ll.label;
      document.getElementById('lossHint').textContent  = ll.hint;

      document.getElementById('summaryText').textContent =
        `${r.duration}h · ${r.weight} kg · ${r.temp}°C · ${zl.label} · ${r.carbsph} g/h carbs · ${r.saltiness} mg/L sodium`;

      // Raw data
      document.getElementById('waterOut').textContent  = r.water.toFixed(1) + ' L';
      document.getElementById('carbsOut').textContent  = Math.round(r.carbs) + ' g';
      document.getElementById('sodiumOut').textContent    = Math.round(r.sodium) + ' mg';
      document.getElementById('sodiumRateOut').textContent = Math.round(r.sodium / r.duration) + ' mg/h via sweat';
      document.getElementById('potassiumOut').textContent  = Math.round(r.potassium) + ' mg';
      document.getElementById('sweatOut').textContent      = `sweat ${r.sweat.toFixed(2)} L/h`;
      document.getElementById('carbsRateOut').textContent  = `${r.duration*r.carbsph*4} kcal · ${r.carbsph * 4} kcal/h`;

      // Nutrition plan
      const plan = buildPlan(r.water, r.carbs, r.sodium, r.potassium, r.duration, r.weight);
      const list = document.getElementById('planList');
      list.innerHTML = plan.items.map(item => {
        if (item.divider) return `<li class="plan-section-divider">${item.divider}</li>`;
        if (item.alt)     return `<li class="plan-alt-divider">${item.alt}</li>`;
        if (item.note)    return `<li class="text-muted fst-italic">${item.note}</li>`;
        if (item.text)    return `<li>${item.text}</li>`;
        if (item.totals) {
          const t = item.totals;
          const chips = [];
          if (t.carbs     > 0) chips.push(`<div class="totals-chip"><span class="totals-chip-value text-success">${Math.round(t.carbs)} g</span><span class="totals-chip-label">carbs · ${Math.round(t.carbs * 4)} kcal</span></div>`);
          if (t.water_ml  > 0) chips.push(`<div class="totals-chip"><span class="totals-chip-value text-primary">${(t.water_ml / 1000).toFixed(1)} L</span><span class="totals-chip-label">water</span></div>`);
          if (t.sodium    > 0) chips.push(`<div class="totals-chip"><span class="totals-chip-value" style="color:#c87000">${Math.round(t.sodium)} mg</span><span class="totals-chip-label">sodium</span></div>`);
          if (t.potassium > 0) chips.push(`<div class="totals-chip"><span class="totals-chip-value" style="color:#7a3fa0">${Math.round(t.potassium)} mg</span><span class="totals-chip-label">potassium</span></div>`);
          return `<li class="plan-totals-row"><span class="totals-heading">Total</span><div class="totals-grid">${chips.join('')}</div></li>`;
        }
        return `<li>
          <span class="plan-item-qty">${item.qty}</span> ${item.unit}
          ${item.details ? `<br><small class="text-muted">${item.details}</small>` : ''}
        </li>`;
      }).join('');

      // Potassium coverage badge in requirements card
      const pct = plan.potassium_target > 0
        ? Math.round((plan.potassium_covered / plan.potassium_target) * 100)
        : 100;
      const pColor = pct >= 60 ? '#198754' : pct >= 30 ? '#c87000' : '#dc3545';
      document.getElementById('potassiumCoverageOut').innerHTML =
        `plan covers <strong style="color:${pColor}">${pct}%</strong> of loss (${Math.round(plan.potassium_covered)} mg)`;

      // Water stops
      const stops = buildStops(r.water, r.capacity, r.duration);
      const stopsBody = document.getElementById('stopsBody');
      const showRecipe = planMode === 'galatico' && r.water > 0 && r.bottles_ml > 0;
      const citrate_g   = plan.citrate_g  ?? 0;
      const glucose_g   = plan.glucose_g  ?? 0;
      const fructose_g  = plan.fructose_g ?? 0;
      const per_L = showRecipe ? 1 / r.water : 0;
      const recipeChips = [];
      if (showRecipe && glucose_g  > 0) recipeChips.push(`<span class="recipe-chip recipe-chip--malto">${Math.round(glucose_g  * per_L)} g <span class="recipe-chip__label">malto</span></span>`);
      if (showRecipe && fructose_g > 0) recipeChips.push(`<span class="recipe-chip recipe-chip--fructose">${Math.round(fructose_g * per_L)} g <span class="recipe-chip__label">fructose</span></span>`);
      if (showRecipe && citrate_g  > 0) recipeChips.push(`<span class="recipe-chip recipe-chip--citrate">${(citrate_g * per_L).toFixed(1)} g <span class="recipe-chip__label">citrate</span></span>`);
      const recipeHtml = recipeChips.length
        ? `<div class="bottle-recipe mt-2">
             <div class="bottle-recipe__label">Bottle Mix (per 1 L)</div>
             <div class="bottle-recipe__chips">${recipeChips.join('')}</div>
           </div>`
        : '';
      const capacity_L = r.capacity / 1000;
      const coverageHtml = `
        <div class="d-flex justify-content-between align-items-baseline mb-2">
          <span class="text-muted small">Need</span>
          <strong>${r.water.toFixed(1)} L</strong>
          <span class="text-muted small">Capacity</span>
          <strong>${capacity_L.toFixed(1)} L</strong>
        </div>`;
      if (!stops) {
        stopsBody.innerHTML = `
          ${coverageHtml}
          <p class="mb-0 text-success fw-semibold">✅ No stops needed</p>
          ${recipeHtml ? '<hr class="my-2">' + recipeHtml : ''}`;
      } else {
        stopsBody.innerHTML = `
          ${coverageHtml}
          <div class="d-flex align-items-center gap-3 mb-1">
            <div class="stops-big text-primary" style="min-width:2.5rem;text-align:center">${stops.stops}</div>
            <div style="min-width:0">
              <div class="fw-bold">water stop${stops.stops > 1 ? 's' : ''}</div>
              <div class="text-muted small" style="white-space:nowrap">every ~${stops.interval_min} min</div>
            </div>
          </div>
          ${recipeHtml ? recipeHtml : ''}`;
      }
    }

    // ── Share ────────────────────────────────────────────────────────

    function share() {
      const r = calculate();
      const p = new URLSearchParams({
        duration: r.duration, carbs: r.carbsph, weight: r.weight,
        temp: r.temp, bottles: r.bottles_ml, pack: r.pack_ml, saltiness: r.saltiness,
        zone: r.zone, loss: r.lossPercent,
        mode: planMode
      });
      const url = location.origin + location.pathname + '?' + p.toString();
      const btns = [document.getElementById('shareBtn'), document.getElementById('shareBtn2')];
      const confirm = () => {
        btns.forEach(b => { b.textContent = '✅ Copied!'; });
        setTimeout(() => btns.forEach(b => { b.textContent = '🔗 Share'; }), 1500);
      };
      if (navigator.clipboard) {
        navigator.clipboard.writeText(url).then(confirm);
      } else {
        // Fallback: show URL in a prompt so the user can copy it manually
        // (execCommand is deprecated; prompt() works everywhere including HTTP)
        window.prompt('Copy this link:', url);
      }
    }

    // ── Event listeners ──────────────────────────────────────────────

    ['durationSlider', 'carbsSlider', 'weightSlider', 'tempSlider', 'bottlesSlider', 'packSlider', 'saltinessSlider', 'zoneSlider', 'lossSlider']
      .forEach(id => document.getElementById(id).addEventListener('input', update));

    // ── Restore from URL params ──────────────────────────────────────

    // Clamp a value to a slider's min/max/step to prevent bad URL params from
    // causing undefined lookups (e.g. CARBS_LABELS[25] or ZONE_LABELS[0]).
    function clampToSlider(id, raw) {
      const el = document.getElementById(id);
      const min  = parseFloat(el.min);
      const max  = parseFloat(el.max);
      const step = parseFloat(el.step);
      let v = parseFloat(raw);
      if (isNaN(v)) return;
      v = Math.min(max, Math.max(min, v));
      // Snap to nearest valid step
      v = Math.round((v - min) / step) * step + min;
      el.value = v;
    }

    const PARAM_MAP = {
      duration: 'durationSlider', carbs: 'carbsSlider', weight: 'weightSlider',
      temp: 'tempSlider', bottles: 'bottlesSlider', pack: 'packSlider', saltiness: 'saltinessSlider',
      zone: 'zoneSlider', loss: 'lossSlider'
    };
    const params = new URLSearchParams(window.location.search);
    Object.entries(PARAM_MAP).forEach(([key, id]) => {
      if (params.has(key)) clampToSlider(id, params.get(key));
    });
    if (params.has('mode') && ['peba','bruto','galatico'].includes(params.get('mode')))
      setMode(params.get('mode')); // setMode calls update()
    else update();
  </script>

  <script>
    document.getElementById('nav-toggle').addEventListener('click', function() {
      const navList = document.getElementById('nav-list');
      const expanded = this.getAttribute('aria-expanded') === 'true';
      this.setAttribute('aria-expanded', String(!expanded));
      navList.classList.toggle('open');
    });
  </script>
</body>
</html>
