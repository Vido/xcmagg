# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Sensitive environment variables
.env
 
# Data (DBs — local only, never commit)
data/
public/data.jsonl

# Django
web/staticfiles/
web/.pytest_cache/

# IDE
.vscode
*.sw*

# MCP config (contains Bearer token — copy from skills/mcp.json.example)
.mcp.json

# Claude Code local config — personal, not versioned (skills live in skills/)
.claude/

# Local dev overrides — never deploy to prod
infra/docker-compose.override.yaml

# blog filebased cache (regenerable)
data/cache/
